{
  "name": "chromatic-tile-transport",
  "version": "3.1.0",
  "private": true,
  "description": "Offline image-driven tile transitions; no runtime npm dependencies",
  "scripts": {
    "test": "node tests/test_matcher.cjs && node tests/test_timing.cjs"
  },
  "engines": { "node": ">=18" }
}
